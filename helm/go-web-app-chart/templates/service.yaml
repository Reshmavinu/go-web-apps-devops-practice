# Service for the application
apiVersion: v1
kind: Service
metadata:
  name: go-web-apps-devops-practice
  labels:
    app: go-web-apps-devops-practice
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: go-web-apps-devops-practice
  type: ClusterIP
