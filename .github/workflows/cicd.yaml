name: CI/CD

on:
  push:
    branches: [main]
    paths-ignore:
      - 'helm/**'
      - 'k8s/**'
      - 'README.md'	  
concurrency:
  group: prod-deploy
  cancel-in-progress: false

env:
  IMAGE_NAME: go-web-apps-devops-practice
  REGISTRY: docker.io

jobs:

  # ---------------------------
  # 1. Build & Test
  # ---------------------------
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.22
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test ./... -v

      - name: Build binary
        run: CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o main

  # ---------------------------
  # 2. Lint (blocking)
  # ---------------------------
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run linter
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest

  # ---------------------------
  # 3. Docker Build & Push
  # ---------------------------
  docker:
    runs-on: ubuntu-latest
    needs: [build, lint]

    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/go-web-apps-devops-practice
          tags: |
            type=sha
            type=raw,value=latest

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/go-web-apps-devops-practice:cache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/go-web-apps-devops-practice:cache,mode=max

  # ---------------------------
  # 4. Deploy to DEV
  # ---------------------------
  deploy-dev:
    runs-on: ubuntu-latest
    needs: docker
    environment: nonprod

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITOPS_TOKEN }}

      - name: Install yq
        run: sudo snap install yq

      - name: Update Helm (nonprod)
        run: |
          yq e -i '.image.tag = "${{ github.sha }}"' helm/go-web-app-chart/values.yaml

      - name: Commit & push
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add .
          git commit -m "Deploy nonprod: ${{ github.sha }}"
          git push
